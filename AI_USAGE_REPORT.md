AI TOOL USED: Gemini[cite: 2]

WHAT I ASKED AI TO DO:[cite: 2]
1. Scaffold the project structure and baseline SQLAlchemy models with strict integer-paise types.
2. Implement the append-only ledger service, dynamic outstanding balance calculator, and payment state machine.
3. Draft the Pytest suite to cover the 9 required edge cases and debug local Python 3.14 environment errors.

PROMPT THAT WAS MOST USEFUL:[cite: 2]
"Assess this file and we shall start building. Walk me through setting up the core ledger and outstanding calculation service focusing on the append-only constraint."

CODE GENERATED BY AI: What part?[cite: 2]
The SQLAlchemy models (`app/models.py`), core ledger logic (`app/services/ledger.py`), payment state machine (`app/services/payments.py`), UI templates, and the Pytest suite (`tests/test_core.py`).

CODE I MODIFIED: What part?[cite: 2]
Adjusted dependency versions in `requirements.txt` to unpin FastAPI and Pydantic versions to resolve local environment conflicts, and patched import paths (`MismatchType`, `get_current_user_role`) across `app/models.py` and `app/deps.py` after splitting the files.

AI OUTPUT THAT WAS WRONG:[cite: 2]
The initial `requirements.txt` provided by the AI contained strictly pinned package versions (e.g., SQLAlchemy 2.0.28, Pydantic 2.6.4). Because I was using Python 3.14 locally, pip attempted to compile `pydantic-core` using a Rust compiler, which failed due to missing Visual C++ Build Tools (`link.exe`). Furthermore, the AI missed exporting `MismatchType` and `get_current_user_role` in the initial file generation blocks.

HOW I IDENTIFIED THE PROBLEM:[cite: 2]
1. `pip install` threw a fatal error: `linker link.exe not found... maturin failed`.
2. Running the Pytest suite threw `ImportError: cannot import name 'MismatchType' from 'app.models'` and `ImportError: cannot import name 'get_current_user_role' from 'app.deps'`.

HOW I FIXED IT:[cite: 2]
I unpinned the dependency versions in `requirements.txt` to pull the latest releases that are compatible with Python 3.14. I then explicitly defined the missing Enum (`MismatchType`) in `app/models.py` and properly implemented the dependency injection functions in `app/deps.py` to ensure the test suite passed successfully.
